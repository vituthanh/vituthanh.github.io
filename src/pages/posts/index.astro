---
import { getCollection } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";
import FormattedDate from "../../components/FormattedDate.astro";
import { Image } from "astro:assets";
const pageTitle = "Danh sách bài viết";
const posts = (await getCollection("blog")).sort((a: { data: { pubDate: { valueOf: () => any; }; }; }, b: { data: { pubDate: { valueOf: () => any; }; }; }) => {
  return b.data.pubDate.valueOf() - a.data.pubDate.valueOf();
});
---
<BaseLayout pageTitle={pageTitle}>
    <!-- <ul>
        {
          posts.map((post: { slug: any; data: { heroImage: any; title: string | null; pubDate: any; }; }) => (
            <li>
              <a href={`/blog/${post.slug}/`}>
                <Image
                  src={post.data.heroImage}
                  height={220}
                  alt={post.data.title}
                />
                <h4 class="title">{post.data.title}</h4>
                {post.data.pubDate && (
                  <p class="date">
                    <FormattedDate date={post.data.pubDate} />
                  </p>
                )}
              </a>
            </li>
          ))
        }
      </ul> -->
</BaseLayout>